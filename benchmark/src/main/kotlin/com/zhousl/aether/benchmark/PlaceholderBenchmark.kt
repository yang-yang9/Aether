package com.zhousl.aether.benchmark

/**
 * Phase 1 placeholder so `:benchmark:compileDebugKotlin` has a source to
 * compile. P7 will replace / supplement this with `StartupBenchmark` and
 * `ScrollBenchmark` (Macrobenchmark `MacrobenchmarkRule` driven).
 */
internal val placeholder = "aether-benchmark"
