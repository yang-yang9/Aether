<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Test-only activities for the native-frameworks POC. They live in
         app/src/androidTest/ so they are only present in the test APK,
         not the production APK. -->
    <application>
        <activity
            android:name="com.zhousl.aether.test.WebViewTestActivity"
            android:exported="false" />
        <activity
            android:name="com.zhousl.aether.test.BridgeWebViewTestActivity"
            android:exported="false" />
    </application>
</manifest>
